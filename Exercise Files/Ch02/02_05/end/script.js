/*
 * Challenge: Create JavaScript comments
 * Edit this script.js file to
 * -Use single-line syntax for single lines only
 * -Use multiline syntax with leading asterisks
 * -Include leading spaces in comments
 * -Enforce strict mode
 * Edit .eslintrc.js to enforce each of the items above
 */

'use strict';

// App setup
const departments = ['produce', 'dairy', 'meat', 'bakery', 'beverages', 'packaged'];
console.log(departments);